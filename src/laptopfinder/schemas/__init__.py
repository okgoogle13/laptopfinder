"""Canonical JSON Schemas for laptopfinder Stage 1 / Stage 1A / Stage 2.

Packaged as a subpackage (rather than a loose top-level schemas/
directory) so the schema files travel with the installed laptopfinder
package and are resolvable via importlib.resources regardless of the
installation method (editable install, wheel, sdist).
"""
