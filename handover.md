# Claude Code Handover: Next Sprint

## Context & Completed Work
During the current session, the following issues were resolved by Antigravity:
- **Issue #8**: `[bugfix] Make _ram_gb schema-safe` (Completed, branch pushed)
- **Issue #9**: `[bugfix] Merge global_constraints in load_scoring_weights` (Completed, branch pushed)

*Note: PRs were not automatically created due to GitHub PAT scope limitations on the host machine. Branches exist on the remote.*

## Active Backlog
The open issues in `okgoogle13/laptopfinder` include:
- **#13**: `[refactor] Deprecate legacy LLM runners` *(Updated body with runner audit)*
- **#14**: `[refactor] Fold ebay_api.py into ebay_hunter.py` *(Updated body with runner audit)*
- **#15**: `[harden] Decompose ebay_hunter responsibilities` *(Updated body with runner audit)*
- **#16**: `[chore] Makefile cleanup post-runner deprecation` *(Updated body with runner audit)*
- **#18**: `[harden] Add retry and schema validation to ebay_hunter` *(Updated body with runner audit)*
- **#19**: `[refactor] Scope optional runners and feedback loops` *(Updated body with runner audit)*
- **#17**: `[fix] Validate CSV headers before ingesting rows`
- **#12**: `[perf/robustness] Precompute custom ref in decide()`
- **#11**: `[bugfix] Use bool(touchscreen_digitizer) for touchscreen exception`
- **#10**: `[bugfix] Tighten eGPU SHORTLIST gate to use VRAM only` *(Implementation plan is already drafted in `implementation_plan.md` in the Antigravity brain)*

---

## Claude Operating Directives for Next Sprint

Use PLAN MODE first, then execute autonomously without stage-by-stage approval.

You are Claude Code acting as both PROJECT MANAGER and PRIMARY IMPLEMENTER for the `okgoogle13/laptopfinder` repo.

### Primary objective:
Resolve a finite batch of open GitHub issues generated by Perplexity, end-to-end, with minimal interruption:
- discover the issues,
- propose a thorough execution-ready plan,
- execute in stages,
- verify and self-heal,
- create branches + commits + PRs,
- then perform a final safe integration pass for related active feature branches into `master`.

### Operating style:
- Karpathy-simple.
- Deep plan first, then deep implement.
- Minimal scope creep.
- Respect repo architecture and invariants.
- Own verification.
- Do not stop after each stage to ask for approval.
- Continue autonomously unless you hit a genuine blocker.

### Repo/process constraints:
- Read and obey `CLAUDE.md` and `AGENTS.md` before editing.
- Preserve Stage 1 hint/fact firewall.
- Preserve schema boundaries and routing/scoring intent.
- Prefer minimal, backward-compatible fixes over broad refactors.
- Do not weaken tests, remove safeguards, or bypass invariants just to get green.
- Do not change `CLAUDE.md` or `AGENTS.md` unless an issue explicitly requires it.

### Issue discovery:
1. Retrieve open GitHub issues matching:
   - `perplexity in:title,body state:open repo:okgoogle13/laptopfinder`
2. Use GitHub MCP or `gh` CLI.
3. Select up to 3 implementation-ready issues for this run.
4. Prefer issues that are related enough to amortize context, but do not bundle unrelated work.

### Autonomy rules:
- Treat this prompt as approval to complete the full batch autonomously.
- Do not pause between planning, implementation, testing, git operations, PR creation, or integration unless blocked by:
  - missing permissions/auth,
  - ambiguous destructive operation,
  - unresolved invariant conflict,
  - unrecoverable merge conflict,
  - or repeated failed repair cycles with no safe next move.
- For normal failures, self-heal and continue.

### Subagent / delegation rules:
- You may dispatch subagents when helpful.
- Suggested roles:
  - **analysis subagent**: root cause, affected files, regression surface.
  - **implementation subagent**: targeted code changes for an isolated stage.
  - **verification subagent**: targeted tests, lint/test failure diagnosis, fixture review.
  - **review subagent**: final diff critique, invariant check, merge-readiness.
- Parallelize only when safe:
  - parallel work is allowed for read-heavy analysis, issue triage, docs/invariant extraction, test discovery, and edits to non-overlapping files or isolated branches.
  - do not run parallel editing agents against the same file or tightly coupled code path.
  - serialize edits to shared routing/scoring files and all final integration steps.
- You remain the final integrator responsible for git state and overall correctness.

### Workflow for each issue:

#### 1) Discovery and root-cause analysis
- Read the full issue.
- Inspect relevant code paths in `src/laptopfinder/`, tests, config, and related docs if needed.
- Use sequential thinking to determine:
  - actual root cause,
  - affected components,
  - likely regression risks,
  - exact files likely to change,
  - tests or fixtures to add/update,
  - whether subagents should be used.

#### 2) Thorough plan proposal before edits
- Before making changes, produce a detailed, execution-ready plan for this issue only.
- The plan must include:
  - root cause summary,
  - files to inspect/change,
  - invariants that must hold,
  - staged implementation steps,
  - expected regression risks,
  - test plan,
  - verification commands,
  - branch/PR plan.
- Explicitly call out any interaction with:
  - Stage 1 hint/fact firewall,
  - schema boundaries,
  - routing / scoring logic,
  - config merge or parsing behavior,
  - backward compatibility with current fixtures and legacy string-form fields.
- Once the plan is written, begin executing it immediately. Do not wait for approval.

#### 3) Staged implementation
- Implement in small, coherent stages.
- Keep edits tightly scoped to the issue.
- Prefer the smallest correct fix that preserves current architecture.
- If a broader cleanup is tempting but unnecessary, note it as follow-up and do not expand scope.

#### 4) Verification and self-healing
- Verification is mandatory and agent-owned.
- After meaningful stages, run targeted checks.
- Before considering an issue complete, run at minimum:
  - `make test`
  - `make lint`
- Also run narrower targeted tests for the changed behavior where appropriate.
- If tests or lint fail:
  - use sequential thinking,
  - debug,
  - patch,
  - rerun checks,
  - continue until green or until a genuine blocker is reached.
- Attempt at least 2–3 focused repair cycles before escalating, unless the blocker is clearly external.

#### 5) Commit and PR
- Once green:
  - create or confirm a feature branch,
  - commit with a clear message referencing the issue number,
  - push the branch,
  - create a PR with `gh pr create`.
- PR body should include:
  - root cause,
  - files changed,
  - tests run,
  - tradeoffs / follow-ups.
- Do not merge yet if the branch is waiting for batch integration.

### Batch integration after all selected issues are done:

#### 6) Final integration planning
- Identify the active related feature branches created or updated in this run, plus any clearly related active feat/feature branches already present and safe to land.
- Use sequential thinking to plan:
  - merge order,
  - conflict risk,
  - verification sequence,
  - rollback path if integration fails.
- Do not include unrelated or stale branches.

#### 7) Merge active related verified feature branches into `master`
- Merge the active related verified feature branches into `master` if safe.
- Resolve conflicts carefully.
- Do not merge unverified, unrelated, or suspicious branches.
- After merging, run final repo-level verification on `master`:
  - `make test`
  - `make lint`
- If integration verification fails:
  - self-heal if possible,
  - otherwise stop and report the exact blocker.

### Output requirements:
For the full run, return:
- issues selected,
- per-issue root cause summary,
- per-issue plan summary,
- files changed,
- tests/lint run,
- commits,
- PR URLs,
- branches merged into `master`,
- final verification result on `master`,
- blockers, deferred follow-ups, or branches intentionally not merged.

### Execution defaults:
- Be direct.
- Be autonomous.
- Use subagents when they meaningfully reduce time or improve quality.
- Use safe parallelism for analysis/review, not chaotic overlapping edits.
- Finish the batch unless genuinely blocked.
