HANDOVER — Evidence Pipeline: Wire Real Gemini Parser
Repo: laptopfinder
Date: 2026-06-28
Environment: Antigravity IDE + integrated terminal

===================================================================
CONTEXT
===================================================================

A manual-handoff evidence pipeline has been scaffolded to derive hardware
target ranges from observed macOS telemetry.

Current flow:
1. Raw telemetry files are dropped into `data/evidence/raw`
2. `src/laptopfinder/runners/evidence_pipeline.py` parses each file into
   normalized JSON and appends to `data/evidence/aggregated.jsonl`
3. Processed files are archived into `data/evidence/archive`
4. Once the record threshold is met, the runner generates
   `data/evidence/claude_handoff.txt`
5. A human later pastes that handoff into Claude Pro manually and saves
   Claude’s JSON response to `data/evidence/targets.json`

The remaining task is to replace the stub in `run_gemini_parser()` with a
real Gemini implementation.

===================================================================
YOUR TASK
===================================================================

Edit only:

  src/laptopfinder/runners/evidence_pipeline.py

Replace the stubbed `run_gemini_parser(file_path: Path) -> dict` with a
real `google-genai` call that accepts a telemetry file and returns a
normalized record matching `evidence_normalized.schema.json`.

Preserve the existing pipeline architecture:
- Do not change the manual Claude Pro handoff design
- Do not add automatic Claude execution
- Do not modify target inference logic outside this parser integration

===================================================================
REPO REFERENCES
===================================================================

Inspect these files before implementing:

- `src/laptopfinder/runners/evidence_pipeline.py`
- `src/laptopfinder/runners/comet.py` if present; mirror its existing
  `.env` loading, Gemini client setup, schema-response pattern, and
  general error handling style
- `src/laptopfinder/schemas/evidence_normalized.schema.json`
- `prompts/gemini_evidence_parser.txt`
- `.env`
- `Makefile`

If `comet.py` exists, follow its established Gemini integration pattern
rather than inventing a new one.

===================================================================
PARSER REQUIREMENTS
===================================================================

The parser must:

1. Detect file type by suffix.
   - Image inputs: `.png`, `.jpg`, `.jpeg`, `.webp`
   - Text inputs: `.txt`, `.log`, `.csv`

2. For image inputs:
   - send the file to Gemini using the repo’s existing `google-genai`
     pattern;
   - use the appropriate image/file content part supported by the SDK.

3. For text inputs:
   - read the file as UTF-8 text;
   - pass the text as model input using the same client pattern.

4. Read the parser instructions from:
   - `prompts/gemini_evidence_parser.txt`

5. Read the output schema 